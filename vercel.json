{
  "installCommand": "yarn install --production=true --ignore-scripts",
  "buildCommand": "yarn run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}